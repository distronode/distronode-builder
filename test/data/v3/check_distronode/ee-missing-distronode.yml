---
version: 3

images:
  base_image:
    name: localhost:8080/testrepo/ubi-minimal:latest

dependencies:
  distronode_runner:
    package_pip: distronode-runner
  python_interpreter:
    package_system: python3

options:
  package_manager_path: /usr/bin/microdnf
